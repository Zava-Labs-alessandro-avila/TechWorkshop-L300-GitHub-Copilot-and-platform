name: zava-storefront
services:
  web:
    project: ./src
    language: dotnet
    host: appservice
    docker:
      path: ./Dockerfile
      context: .
      port: 8080
    azure:
      bicep:
        path: ./infra/main.bicep
        parameters:
          appName: zava-storefront
